---
name: llmfw-testing
description: Load before writing tests, fixtures, or touching coverage gates. Use for "write a test for this", "add a fixture", "why is coverage failing".
---

Read the project's `docs/conventions/testing.md` if present, else
`${CLAUDE_PLUGIN_ROOT}/conventions/testing.md`, before making the change.
